# Secrets & local state — never commit
.env
.env.local
*.env
config/projects.json
data/
dist/
node_modules/
logs/
*.log

# OS / editor
Thumbs.db
.DS_Store
.vscode/settings.json

# Copilot session artifacts
copilot-session-*.md

# Personal launch/marketing notes — never ship with the repo
social.md

# Throwaway repos created by scripts/live-acceptance.mjs (spends real credits)
.live-acceptance-*/
# Scratch workspaces for manual web-UI testing
.web-demo/
.scratch-perm/
